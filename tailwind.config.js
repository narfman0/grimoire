// Tailwind v4 reads most config from CSS via @theme. This file is kept for
// IDE plugins that still expect a JS config and as a place to extend if needed.
/** @type {import('tailwindcss').Config} */
export default {
  content: ['./src/**/*.{html,js,svelte,ts}']
};
