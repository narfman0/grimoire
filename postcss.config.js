// Tailwind v4 integrates with Vite via @tailwindcss/vite, so no PostCSS config
// is required for Tailwind itself. This file is kept as a stub for downstream
// PostCSS plugins (autoprefixer is handled by Vite/Lightning CSS internally).
export default {
  plugins: {}
};
